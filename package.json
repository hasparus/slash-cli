{
  "name": "slash-cli",
  "version": "1.0.0",
  "description": "A CLI for slash",
  "main": "index.js",
  "author": "hasparus",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "check": "yarn build && yarn test",
    "prepublish": "yarn check"
  },
  "dependencies": {
    "slash": "^2.0.0",
    "ts-jest": "^24.0.2"
  },
  "devDependencies": {
    "@types/jest": "^24.0.11",
    "@types/node": "^11.13.4",
    "@types/slash": "^2.0.0",
    "husky": "^1.3.1",
    "jest": "^24.7.1",
    "typescript": "^3.4.3"
  },
  "husky": {
    "hooks": {
      "pre-push": "yarn check"
    }
  },
  "@pika/pack": {
    "pipeline": [
      [
        "@pika/plugin-ts-standard-pkg"
      ],
      [
        "@pika/plugin-build-node"
      ],
      [
        "@pika/plugin-bundle-node"
      ]
    ]
  }
}
